{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build GTKMM App",
            "type": "shell",
            "command": "g++",
            "args": ["-o", "bin/main", "src/main.cpp", "`pkg-config --cflags --libs gtkmm-4.0`"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}